export const analysis = async (ctx) => {

  // 1.获取node_models内容 量很大 是否需要防护措施？ 关于循环依赖和深度限制处理
    // 获取依赖的方式可以通过命令行pnpm

  // 2.制定规则进行匹配

  // 3.数据返回 data和line 节点和关系 需要配备 包含节点名 节点大小 节点的source和target
  
  ctx.body = { success: true }
}
